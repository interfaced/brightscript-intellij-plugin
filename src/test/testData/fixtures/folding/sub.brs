sub fn(x, y)<fold text='...'>
    sub a(x, y) as Void<fold text='...'>
        end
    </fold>endsub

    mysub = sub()<fold text='...'>
        print "fred"
    </fold>endsub

    a("foo", "bar")
</fold>end sub

sub main()<fold text='...'> : print "exit" : </fold>endsub